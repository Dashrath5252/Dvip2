name: 🚀 Auto Optimize Images
on:
  push:
    paths:
      - '*.webp'
  workflow_dispatch:

jobs:
  optimize:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3
        
      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: 📦 Install sharp
        run: npm install sharp
        
      - name: 🖼️ Run optimization
        run: node optimize.js
        
      - name: ✅ Success Message
        run: echo "Optimization complete! Check *_google.png and *_optimized.webp files"
